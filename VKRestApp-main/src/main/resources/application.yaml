spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/restApiDataBase
    username: postgres
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update